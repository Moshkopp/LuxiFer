[package]
name = "driver-grbl"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "GRBL/miniGRBL-Treiber für Studio: JobPlan → G-Code. Kennt nur den Core."

[dependencies]
studio-core = { path = "../../core" }
